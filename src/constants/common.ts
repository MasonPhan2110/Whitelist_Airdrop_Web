export const connectorStorage = 'connected';
export const SECTION_ID = {
  ABOUT: '#about',
  CLAIM: '#claim',
  CHECK: '#check',
};

export const CONTRACT_COLLECTION =
  process.env.NEXT_PUBLIC_CONTRACT_COLLECTION || '';
